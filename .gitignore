# Dependencies
node_modules/

# Logs / local state
*.log
legacy/agent/.gotmp/

# Local drafts
xiaohongshu-post.md
*.tgz

# NOTE: lib/ (the built node + client bundles) is intentionally committed so
# the repo installs directly via `dsh plugin add <git url>` without a build step.
